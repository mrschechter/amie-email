import tokens from "./tokens";

// ==============================|| DEFAULT THEME - CUSTOM SHADOWS  ||============================== //

const CustomShadows = () => ({
  button: tokens.shadows.button,
  text: tokens.shadows.text,
  z1: tokens.shadows.medium,
  inset: tokens.shadows.inset,
});

export default CustomShadows;
